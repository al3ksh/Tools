services:
  # API server (serves web UI on port 3000)
  api:
    build:
      context: .
      dockerfile: Dockerfile.web
    ports:
      - "3000:3001"
    volumes:
      - ./data:/data
    env_file:
      - .env
    environment:
      - DATA_DIR=/data
      - PORT=3001
      - NODE_OPTIONS=--max-old-space-size=512
    restart: unless-stopped
    mem_limit: 768m
    cpus: 1.5
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    cap_add:
      - DAC_OVERRIDE
      - NET_BIND_SERVICE
      - SETUID
      - SETGID
    tmpfs:
      - /tmp
      - /root/.npm
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://127.0.0.1:3001/api/health"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s

  # Worker
  worker:
    build:
      context: .
      dockerfile: Dockerfile.worker
    volumes:
      - ./data:/data
    env_file:
      - .env
    environment:
      - DATA_DIR=/data
      - NODE_OPTIONS=--max-old-space-size=1024
    depends_on:
      api:
        condition: service_healthy
    restart: unless-stopped
    mem_limit: 1536m
    cpus: 2
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    cap_add:
      - DAC_OVERRIDE
      - SETUID
      - SETGID
    tmpfs:
      - /tmp
      - /root/.npm
    healthcheck:
      test: ["CMD-SHELL", "node -e \"try { require('better-sqlite3')('/data/tools.db').prepare('SELECT 1').get(); process.exit(0) } catch(e) { process.exit(1) }\""]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s

volumes:
  data:
